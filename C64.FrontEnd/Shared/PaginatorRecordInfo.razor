<div>@TotalNumberOfRecords Results / Showing Page @CurrentPage of @NumberOfPages  with records @StartRecord to @EndRecord</div>
@code {

    [Parameter]
    public int CurrentPage { get; set; }

    [Parameter]
    public int PageSize { get; set; }

    [Parameter]
    public int TotalNumberOfRecords { get; set; }

    private int StartRecord
    {
        get
        {
            return (CurrentPage - 1) * PageSize + 1;
        }
    }

    private int EndRecord
    {
        get
        {
            var tmp = CurrentPage * PageSize;
            return Math.Min(tmp, TotalNumberOfRecords);
        }
    }

    private int NumberOfPages
    {
        get
        {
            return (int)((TotalNumberOfRecords - 1) / PageSize) + 1;
        }
    }
}